@model Hobby
<p>Hello @ViewBag.User.FirstName! <a asp-action="Dashboard" asp-controller="Exam">Dashboard</a> - <a
        asp-action="LogOut" asp-controller="Home">LogOut</a></p>
    <hr>
<div class="row">
    <div class="col-md-6">
        <h3>@Model.Name</h3>
        Description :<p >  @Model.Description</p>

        <h5> @Model.Name Enthusiasts:</h5>
        <ul>
            @foreach (var h in Model.Hlikes)
            {
                <li>@h.Hlike.FirstName</li>
            }
            <hr>
        </ul>
        @if (Model.Amateur.UserId == ViewBag.User.UserId)
        {
            <a asp-action="Edit" asp-controller="Exam" asp-route-hobbyId="@Model.HobbyId"><button
                class="btn btn-sm btn-success">Edit Hobby</button></a>
            @* <a asp-action="Delete" asp-controller="Exam" asp-route-hobbyId="@Model.HobbyId"><button
                class="btn btn-sm btn-danger">Delete</button></a> *@
        }
        else if (Model.Hlikes.Any(f => f.UserId == ViewBag.User.UserId))
        {
            @* <a asp-action="Unlike" asp-controller="Exam" asp-route-hobbyId="@Model.HobbyId"><button
                class="btn btn-sm btn-warning">Unlike</button></a> *@
    <a asp-action="New" asp-controller="Exam" class="btn btn-sm btn-warning">Add Your Hobby</a>

        }
        else
        {
                <h6>Become an Enthusiast</h6>
            <a asp-action="Join" asp-controller="Exam" asp-route-hobbyId="@Model.HobbyId"><button
                class="btn btn-sm btn-primary">Add To Hobbies</button></a>
        }
    </div>
</div>